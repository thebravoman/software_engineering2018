Name:<%= @person.name%> <br/>
Subscribed events: <br/>
<% @events_subscribed.each do |event| %>
    name: <%= event.name%><br/>
    location: <%= event.location%><br/>
    date: <%= event.date%><br/>
    description: <%= event.description%><br/>
    <%= link_to "Unsubscribe", "/person/#{@person.id}/unsubscribe/#{event.id}", method: :delete %> <br/>
<% end %>

Events you may like: <br/>
<%= form_for @attendance, url: "/person/#{@person.id}/subscribe/" do |f| %>
    <%= f.hidden_field :person_id, value: @person.id %>
    <%= f.select :event_id, @events_unsubscribed.map{|event| [event.name, event.id]} %>
    <%= f.submit "Subscribe to event" %>       
   
    
<% end %>
  

