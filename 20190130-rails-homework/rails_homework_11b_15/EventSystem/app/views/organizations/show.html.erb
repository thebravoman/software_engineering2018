<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @organization.name %>
</p>

<div class="wrapper">
  <% unless @subscribed.empty? %>
    <div class="personal subscribed">
    <h3> Personal </h3>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @members.each do |member| %>
            <tr>
              <td><%= memeber.name %></td>
              <td><%= link_to 'Expell', "/organizations/#{@organization.id}/expell_memeber", method: :delete %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% end %>
</div>

<div id="add-member">

  <%= form_with(url: "/organizations/#{@organization.id}/add_person", local: true) do |f| %>
    <%= f.select :member_id, @nonmembers, :prompt => 'Select One' %>
    <%= f.submit "Add member" %>
  <% end %>

</div>

<%= link_to 'Edit', edit_organization_path(@organization) %> |
<%= link_to 'Back', organizations_path %>
